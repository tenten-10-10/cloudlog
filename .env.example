# Deployment (Docker + Caddy)
DOMAIN=example.com
# Optional, but recommended for ACME registration
EMAIL=you@example.com

# Telegram (recommended: phone + PC both)
# 1) Create a bot with @BotFather
# 2) Get your chat_id (e.g. by sending a message and checking getUpdates)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Pushover (optional)
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=

# Web UI auth (required for public deployment)
SITEWATCHER_SECRET_KEY=
SITEWATCHER_ALLOWED_HOSTS=example.com
SITEWATCHER_HTTPS_ONLY=1
# Allow public sign-ups (set 0 for invite-only)
SITEWATCHER_ALLOW_REGISTRATION=1
SITEWATCHER_ADMIN_USER=admin
# Preferred:
SITEWATCHER_ADMIN_PASSWORD_HASH=
# Or (not recommended):
# SITEWATCHER_ADMIN_PASSWORD=

# Security: block localhost/private IPs by default
SITEWATCHER_ALLOW_PRIVATE_NETWORK=0
